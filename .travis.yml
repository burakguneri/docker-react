language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t burakguneri/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true burakguneri/docker-react npm run test